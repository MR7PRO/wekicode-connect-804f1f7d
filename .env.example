# Supabase Configuration
# Copy this file to .env and fill in your values

# Required: Your Supabase project URL
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# Required: Your Supabase anon/public key
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"

# Required: Your Supabase project ID
VITE_SUPABASE_PROJECT_ID="your-project-id"

# =====================================================
# DOCKER DEPLOYMENT
# =====================================================
# When building with Docker, pass these as build args:
#
# docker build \
#   --build-arg VITE_SUPABASE_URL="https://your-project-id.supabase.co" \
#   --build-arg VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key" \
#   --build-arg VITE_SUPABASE_PROJECT_ID="your-project-id" \
#   -t wekicode .
#
# Then run:
# docker run -p 80:80 wekicode

# =====================================================
# NETLIFY DEPLOYMENT
# =====================================================
# Set these environment variables in Netlify dashboard:
# Site settings > Build & deploy > Environment variables
#
# VITE_SUPABASE_URL
# VITE_SUPABASE_PUBLISHABLE_KEY
# VITE_SUPABASE_PROJECT_ID

# =====================================================
# NOTES
# =====================================================
# - Edge functions are deployed separately on Supabase
# - The backend (database, auth, edge functions) runs on Supabase
# - Only the frontend (React app) needs to be deployed on Netlify/Docker
# - Make sure your Supabase project is accessible from your deployment domain
